// json-server : start
json-server --watch db.json --host 0.0.0.0 --port 5000
# Change the url in main.js example -http://3.75.206.42:5000/students  for students  same for admin and faculty
# change the URL "localhost:3000" to "your-generated-ip"
